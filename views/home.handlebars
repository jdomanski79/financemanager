<div class="card fluid shadowed">
   <h1 class="section">
     Witaj {{user.name}}!
   </h1>
    <div class="row">
      <div class="col-sm-12">
        <h2>
          Bilans miesiąca: {{bilans}} zł
        </h2>  
      </div>
      <div class="col-sm-12 col-md-6">
        <div class="card fluid shadowed">
          <h2 class="section">
            Wpływy: {{data.incomes[0].total}} zł
          </h2>
<!--           <ul>
            {{#each data.incomes[0].categories}}
            <li>
              {{this.name}} - {{this.sum}} zł
            </li>
            {{/each}}
          </ul> -->
        </div>
      </div>

  </div>
</div>

<div class="card fluid shadowed">
   <h1 class="section">
     Witaj {{user.name}}!
   </h1>
    <div class="row">
      {{#each data}}
        <div class="col-sm-12 col-md-6">
          <div class="card fluid shadowed">
            <h2 class="section">
              {{this._id}}
            </h2>
            <ul>
              {{#each this.categories}}
              <li>
                {{this.name}} - {{this.sum}} zł
              </li>
              {{/each}}
            </ul>
          </div>
        </div>
      {{/each}}
  </div>
</div>
